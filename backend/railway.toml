[build]
builder = "DOCKERFILE"

[deploy]
restartPolicyType = "ON_FAILURE"

# Reference the PostgreSQL service for DATABASE_URL
[[build.buildArgs]]
name = "DATABASE_URL"
value = "${{Postgres.DATABASE_URL}}"
